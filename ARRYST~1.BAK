#include<stdio.h>
#include<conio.h>
struct student
{
	char name[10];
	int roll - no;
	int marks;
}s[5];
void main()

{
	int i;
	clrscr();
	for (i = 0;i < 5;i++)
	{
		printf("Enter the name,roll-no and marks of the student");
		scanf("%s%d%d", &s[i].name, &s[i].roll - no, &s[i].marks);
	}
	for (i = 0;i < 5;i++)
	{
		printf("\n%s%d%d", s[i].name, s[i].roll - no, s[i]marks);
	}
	getch();
}